export const workerPhase = "phase-1-placeholder";

export function describeWorkerPlaceholder(): string {
  return "Phase 1 reserves the worker package for upload grouping, PhotomatixCL jobs, and exports. No worker runtime is implemented yet.";
}
