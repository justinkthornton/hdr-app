{
  "name": "@structure-locked-hdr/worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "lint": "eslint .",
    "smoke:fake": "tsx src/smoke.ts fake",
    "smoke:photomatix": "tsx src/smoke.ts photomatix",
    "test": "vitest run packages/worker"
  },
  "dependencies": {
    "@structure-locked-hdr/core": "workspace:*"
  }
}
